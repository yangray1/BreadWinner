package com.example.crystalyip.csc301.AsyncTasks;

import android.os.AsyncTask;

import java.net.URL;

public class SearchAllOrders extends AsyncTask<URL, Integer, Long> {
    @Override
    protected Long doInBackground(URL... urls) {
        return null;
    }
}
